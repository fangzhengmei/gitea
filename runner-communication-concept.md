# 自托管运行器与服务端通信机制概念报告

## 1. 概述

自托管运行器（Self-hosted Runner）是CI/CD系统中部署在用户自有环境中的任务执行节点。本文档从概念层面阐述运行器与服务端之间的核心通信机制，包括任务分发、日志回传、状态上报、鉴权、租约管理及异常恢复。

## 2. 架构概览

### 2.1 角色定义

**服务端（Server）**
- 负责任务调度与分发
- 维护运行器注册信息与状态
- 接收并存储执行日志与任务结果
- 管理运行器生命周期

**运行器（Runner）**
- 向服务端注册并保持在线状态
- 轮询或接收任务分配
- 执行CI/CD任务
- 实时回传执行日志与状态更新

### 2.2 通信模式

运行器与服务端采用**"运行器主动拉取"**为主的通信模式：
- 运行器主动向服务端发起连接请求
- 所有通信由运行器发起，服务端仅响应请求
- 这种设计便于运行器部署在防火墙/NAT后

## 3. 鉴权机制

### 3.1 注册阶段鉴权

运行器首次连接时需要完成注册流程：

1. **令牌获取**：管理员在服务端生成注册令牌（Registration Token），该令牌具有时效性和使用次数限制
2. **注册请求**：运行器使用注册令牌向服务端发起注册请求，附带运行器基本信息（名称、标签、架构、操作系统等）
3. **身份颁发**：服务端验证令牌有效后，为运行器颁发唯一身份凭证（Runner Token/Runner ID）
4. **凭证存储**：运行器安全存储身份凭证，后续所有通信均使用该凭证进行认证

### 3.2 日常通信鉴权

运行器与服务端的每次交互都需要进行身份验证：

- **请求签名**：运行器使用身份凭证对请求进行签名
- **令牌过期**：服务端可配置令牌有效期，过期后需要重新认证
- **权限隔离**：每个运行器只能访问分配给它的任务，无法查看其他运行器的任务信息

## 4. 任务分发机制

### 4.1 任务队列模型

服务端维护全局任务队列，采用"生产者-消费者"模型：

1. **任务入队**：当触发CI/CD流水线时，服务端将任务拆解为可执行单元，加入待执行队列
2. **标签匹配**：每个任务带有标签要求，服务端根据运行器注册时声明的标签进行匹配
3. **任务调度**：服务端根据调度策略（FIFO、优先级、负载均衡）将任务分配给可用运行器

### 4.2 任务拉取流程

运行器通过轮询方式获取任务：

1. **心跳请求**：运行器定期向服务端发送心跳，询问是否有新任务
2. **任务响应**：服务端如果有匹配的任务，将任务详情（代码仓库、执行脚本、环境变量、超时时间等）返回给运行器
3. **任务认领**：运行器接收到任务后，向服务端发送"已认领"确认
4. **状态锁定**：服务端将任务状态标记为"执行中"，并与该运行器绑定，避免重复分配

### 4.3 并发控制

- 每个运行器可配置并发执行任务数
- 服务端根据运行器当前负载决定是否分配新任务
- 运行器达到并发上限时，服务端将任务保留在队列中等待

## 5. 日志回传机制

### 5.1 日志流模型

任务执行过程中产生的日志采用流式回传：

1. **日志分块**：运行器将标准输出/标准错误输出按行或按时间窗口分块
2. **增量上传**：每个日志块带有序列号和时间戳，确保顺序性
3. **实时推送**：运行器将日志块实时上传到服务端，延迟通常在秒级
4. **确认机制**：服务端收到日志块后返回确认，未确认的日志块会重试

### 5.2 日志存储与索引

- 服务端接收日志后进行持久化存储
- 按任务ID、步骤ID建立索引，便于查询
- 支持日志的实时浏览和历史检索

### 5.3 异常处理

- 网络中断时，运行器在本地缓存日志
- 网络恢复后，从断点续传未确认的日志块
- 任务结束后，运行器确保所有日志都已成功上传

## 6. 状态上报机制

### 6.1 运行器状态

运行器定期上报自身健康状态：

- **在线状态**：通过心跳包维持
- **负载信息**：CPU、内存、磁盘使用率
- **任务状态**：当前正在执行的任务列表及进度
- **系统信息**：操作系统版本、运行器版本等

### 6.2 任务执行状态

任务执行过程中，运行器上报详细状态：

1. **任务启动**：任务开始执行时上报"开始"状态
2. **步骤更新**：每个步骤开始/结束时上报步骤状态
3. **进度更新**：长时间运行的任务可定期上报进度百分比
4. **任务完成**：任务执行结束后上报最终状态（成功/失败/取消/超时）

### 6.3 上报策略

- **增量上报**：仅上报发生变化的状态
- **合并上报**：短时间内的多次状态变化可合并为一次上报
- **优先级队列**：任务完成状态优先级高于中间状态

## 7. 租约续约机制

### 7.1 租约概念

运行器认领任务后，获得该任务的执行租约（Lease）：

- 租约是运行器对任务的临时所有权
- 租约有有效期，过期后服务端可回收任务
- 运行器需要在租约过期前进行续约

### 7.2 续约流程

1. **初始租约**：运行器认领任务时获得初始租约（如5分钟）
2. **定期续约**：运行器在任务执行过程中，定期向服务端发送续约请求
3. **续约确认**：服务端确认运行器仍在正常执行任务，延长租约有效期
4. **租约过期**：如果运行器未按时续约，服务端认为任务执行异常

### 7.3 续约策略

- 续约间隔通常为租约有效期的1/3到1/2
- 续约请求可与心跳或日志上传合并
- 续约失败时，运行器进行重试（带退避策略）

## 8. 异常断线与回收机制

### 8.1 异常检测

服务端通过多种方式检测运行器异常：

1. **心跳超时**：运行器超过配置时间未发送心跳
2. **租约过期**：任务执行中的运行器未按时续约
3. **日志中断**：正在执行的任务突然停止日志输出
4. **网络错误**：服务端无法与运行器建立连接

### 8.2 异常分类

| 异常类型 | 触发条件 | 处理策略 |
|---------|---------|---------|
| 短暂失联 | 心跳超时<阈值 | 标记为"可能离线"，等待重连 |
| 任务失联 | 租约过期 | 标记为"执行异常"，触发回收 |
| 永久离线 | 心跳超时>阈值 | 标记为"离线"，从可用运行器列表移除 |

### 8.3 任务回收流程

当检测到运行器异常时，服务端执行任务回收：

1. **状态标记**：将任务状态从"执行中"改为"异常"
2. **资源释放**：释放该任务占用的资源配额
3. **重试决策**：根据任务配置决定是否重新入队重试
4. **通知机制**：通知任务发起者任务执行异常
5. **运行器隔离**：将异常运行器标记为不可用，避免分配新任务

### 8.4 运行器重连

异常运行器恢复后：

1. **重连认证**：运行器使用存储的身份凭证重新认证
2. **状态同步**：运行器向服务端查询之前执行的任务状态
3. **任务恢复**：
   - 如果任务未被回收，继续执行并续约
   - 如果任务已被回收，清理本地执行环境
4. **重新注册**：运行器重新加入可用运行器池

### 8.5 优雅关闭

运行器支持优雅关闭流程：

1. **关闭信号**：运行器接收到关闭信号（如SIGTERM）
2. **停止接收**：停止向服务端请求新任务
3. **等待完成**：等待当前执行的任务完成（可配置超时）
4. **状态上报**：上报最终状态和剩余日志
5. **注销通知**：向服务端发送"正在关闭"通知
6. **资源清理**：清理临时文件和执行环境

## 9. 总结

自托管运行器与服务端的通信机制设计遵循以下核心原则：

1. **可靠性优先**：通过租约、重试、确认等机制确保任务不丢失
2. **安全性保障**：多层鉴权机制防止未授权访问
3. **容错设计**：异常检测与自动回收机制保障系统稳定性
4. **可观测性**：实时状态上报与日志回传便于监控与调试
5. **网络友好**：运行器主动拉取模式适应复杂网络环境

这些机制共同构成了一个健壮、可扩展的分布式任务执行系统。
